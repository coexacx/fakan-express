# ====== App ======
PORT=3000
NODE_ENV=production

# Very important: change these in production
SESSION_SECRET=change_me_session_secret_please
CARD_SECRET=change_me_card_secret_please

# How long to reserve card keys for an unpaid order (minutes)
RESERVE_MINUTES=30

# If you put the app behind HTTPS reverse proxy (recommended), set these:
# TRUST_PROXY=1
# COOKIE_SECURE=true

# If set, the app will create the first admin account on startup (only if no admin exists)
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=admin123456

# Optional: webhook secret for real payment callbacks (HMAC-SHA256)
# PAYMENT_WEBHOOK_SECRET=change_me_webhook_secret

# ====== Database ======
# Recommended: use DATABASE_URL in docker-compose
DATABASE_URL=postgres://postgres:postgres@db:5432/fakan

# If you don't want DATABASE_URL, you can use these instead:
# DB_HOST=127.0.0.1
# DB_PORT=5432
# DB_NAME=fakan
# DB_USER=postgres
# DB_PASSWORD=postgres
