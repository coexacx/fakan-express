<%- include('../partials/header', { title, admin, flash }) %>

<div class="row justify-content-center">
  <div class="col-12 col-md-6 col-lg-4">
    <div class="card admin-card">
      <div class="card-body">
        <h1 class="h5 mb-3 admin-title">商家登录</h1>
        <div class="admin-subtitle mb-3">请输入管理员账号与密码</div>
        <form class="admin-form" method="post" action="/admin/login">
          <div class="mb-3">
            <label class="form-label">账号</label>
            <input class="form-control" name="username" required>
          </div>
          <div class="mb-3">
            <label class="form-label">密码</label>
            <input class="form-control" type="password" name="password" required>
          </div>
          <button class="btn btn-primary w-100" type="submit">登录</button>
        </form>
        <div class="form-text mt-3 admin-muted">
          首次部署可通过 .env 中的 BOOTSTRAP_ADMIN_USERNAME/BOOTSTRAP_ADMIN_PASSWORD 自动创建管理员。
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
