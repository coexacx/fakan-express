<%- include('../partials/header', { title, admin, flash }) %>

<div class="admin-login">
  <div class="card">
    <h1 class="admin-title">商家登录</h1>
    <div class="admin-subtitle">请输入管理员账号与密码</div>
    <form class="admin-form" method="post" action="/admin/login">
      <div class="field">
        <label class="label">账号</label>
        <input class="input" name="username" required>
      </div>
      <div class="field">
        <label class="label">密码</label>
        <input class="input" type="password" name="password" required>
      </div>
      <div class="btnrow">
        <button class="btn primary block" type="submit">登录</button>
      </div>
    </form>
    <div class="help admin-section">
      首次部署可通过 .env 中的 BOOTSTRAP_ADMIN_USERNAME/BOOTSTRAP_ADMIN_PASSWORD 自动创建管理员。
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
