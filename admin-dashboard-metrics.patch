--- a/src/routes/admin.js
+++ b/src/routes/admin.js
@@ -109,16 +109,31 @@
 
 router.get('/', requireAdmin, async (req, res) => {
-  const [{ rows: prodRows }, { rows: keyRows }, { rows: pendingRows }] = await Promise.all([
-    pool.query('SELECT COUNT(*)::int AS c FROM products'),
-    pool.query("SELECT COUNT(*)::int AS c FROM card_keys WHERE status='available'"),
-    pool.query("SELECT COUNT(*)::int AS c FROM orders WHERE status IN ('pending','paid','delivery_failed')"),
-  ]);
+  const [
+    { rows: prodRows },
+    { rows: keyRows },
+    { rows: pendingRows },
+    { rows: todayAmtRows },
+    { rows: totalAmtRows },
+    { rows: paidOrderRows },
+  ] = await Promise.all([
+    pool.query('SELECT COUNT(*)::int AS c FROM products'),
+    pool.query("SELECT COUNT(*)::int AS c FROM card_keys WHERE status='available'"),
+    pool.query("SELECT COUNT(*)::int AS c FROM orders WHERE status IN ('pending','paid','delivery_failed')"),
+    pool.query("SELECT COALESCE(SUM(total_cents),0)::bigint AS cents FROM orders WHERE status IN ('paid','delivered','delivery_failed') AND paid_at >= NOW() - INTERVAL '24 hours'"),
+    pool.query("SELECT COALESCE(SUM(total_cents),0)::bigint AS cents FROM orders WHERE status IN ('paid','delivered','delivery_failed')"),
+    pool.query("SELECT COUNT(*)::int AS c FROM orders WHERE status IN ('paid','delivered','delivery_failed')"),
+  ]);
 
   res.render('admin/dashboard', {
     title: '后台管理',
+    formatMoney,
     stats: {
+      todayRevenueCents: todayAmtRows[0].cents,
+      totalRevenueCents: totalAmtRows[0].cents,
+      totalOrders: paidOrderRows[0].c,
       products: prodRows[0].c,
       availableKeys: keyRows[0].c,
       openOrders: pendingRows[0].c,
     },
   });
 });
 
--- a/src/views/admin/dashboard.ejs
+++ b/src/views/admin/dashboard.ejs
@@ -11,6 +11,30 @@
     </div>
 
     <div class="grid">
+      <div class="card">
+        <div class="card-title">今日成交金额</div>
+        <div class="muted">过去24小时内已支付金额</div>
+        <div class="price">￥<%= formatMoney(stats.todayRevenueCents) %></div>
+        <div class="btnrow">
+          <a href="/admin/orders" class="btn">查看订单</a>
+        </div>
+      </div>
+      <div class="card">
+        <div class="card-title">总成交金额</div>
+        <div class="muted">开业至今累计成交金额</div>
+        <div class="price">￥<%= formatMoney(stats.totalRevenueCents) %></div>
+        <div class="btnrow">
+          <a href="/admin/orders" class="btn">查看订单</a>
+        </div>
+      </div>
+      <div class="card">
+        <div class="card-title">总订单量</div>
+        <div class="muted">开业至今成交订单数量</div>
+        <div class="price"><%= stats.totalOrders %></div>
+        <div class="btnrow">
+          <a href="/admin/orders" class="btn">查看订单</a>
+        </div>
+      </div>
       <div class="card">
         <div class="card-title">商品数量</div>
         <div class="muted">已上架商品数量</div>
